# 分享版验证记录

验证日期：2026-09-15。

## 已验证

- 在 macOS、Python 3.12 的隔离目录（包含空格的路径）完成安装器安装及依赖安装。
- 重复安装会拒绝覆盖已有技能和配置；Python 3.9 会被版本检查拒绝。
- 54 项测试通过：配置解析、排版转换、文字评分、配图入口等；Python 生图请求用模拟响应验证，没有产生付费请求。
- 5 项可选 Bun 集成测试因没有安装 Bun 而跳过。选择 Bun 路线的用户应安装 Bun 后重跑。
- 技能格式验证通过；固定出口脚本语法、无配置帮助、缺配置停止行为通过。
- 离线示例已生成 HTML，确认参考资料字体规格与行内标记转换。
- 按文件类型白名单复制资源；没有携带历史文章、图片、真实 YAML、配置备份、服务器私有配置、Token 缓存或虚拟环境。
- 对包内文件进行了与本机私有配置值的内存比对及私人路径、原服务器地址检查，未发现残留；未输出私有值。

## 使用者仍需验证

- 自己的生图服务是否支持所选模型、尺寸和返回格式。
- 自己公众号的 API 权限、凭证、服务器出口与 IP 白名单。
- 实际草稿上传和回读，以及手机端视觉效果。
- Windows/WSL 和其他 AI 客户端未在本次环境实测。

本次未调用真实生图或微信公众号接口。测试通过不代表可免配置使用第三方服务。
