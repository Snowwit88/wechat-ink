# 微墨 2 验证记录

日期：2026-09-23。状态：本地候选版，尚未完成真实服务验收。

## 已完成

- 34 项自动化测试通过：15 个配色入口、文稿解析、参考资料字体与换行、HTML 转义、图片缺失检查、配图请求格式、图片下载不携带服务密钥、16:9 裁剪、出口拦截、草稿去重、超时保护、参数变更识别、安装隔离和密钥不回显。
- 使用同一份原创样稿及 7 张离线测试图分别运行第一版和新版，生成新旧预览。
- 浏览器检查 390px 对照窗口：新版 7 张图片全部加载；内容区域宽度与滚动宽度同为 373px（扣除边框、滚动条后），未出现横向溢出。
- 目视检查参考资料：来源与 URL 同条目排列，长 URL 自然折行。
- 技能格式验证通过；安装到临时独立目录后成功渲染样稿；重复安装拒绝覆盖。
- 对本次 9 个 Python 实现文件与参考源码快照中的 43 个脚本做逐文件、逐行检查：无完整相同文件，最长连续相同非空行为 3 行。此统计仅排查直接复制，不能单独证明法律上的独立性。

## 实现范围与差异

默认学术主题已按对照结果调整字体、正文大小、标题大小和图片间距。列表、引用、表格仍有视觉差异；其他主题目前是新版配色，不是旧版完整主题复刻。

写作与事实核查通过技能工作流，由当前助手完成。程序负责解析、检查、排版、生图请求与草稿 API，不自动调用文本模型。

未迁入跨平台同步、Cookie 生图、旧版自定义样式 JSON。图片消息 newspic 入口仍属实验状态，不能作为已验证功能宣传。

## 待实际验证

1. 用用户授权的图片服务生成真实图片，核对内容、源尺寸与封面裁剪。
2. 从固定出口上传素材、创建一篇测试草稿并回读。
3. 在微信客户端查看最终排版，与第一版同文稿对照。

本次没有消耗生图额度，没有创建真实微信草稿。v2 以预览版发布到原仓库 main，第一版保存在 legacy-v1；本机已安装的第一版不变。

## 复现

项目根目录安装 `wechat-ink/requirements.txt` 和 pytest 后，运行 `python -m pytest -q`。

本机对照文件位于 `validation/index.html`，图片全部内嵌。该目录包含第一版生成结果，仅作本机对照，不纳入新版安装包或新版 MIT 授权范围。

## 发布体验检查

新增无依赖 demo.py 与自包含 demo.html；使用 Python -S（不加载第三方包）测试入口通过。浏览器已查看完整样稿。README 按效果、体验、创作、可选发布组织，全部示例明确标注预置内容。
